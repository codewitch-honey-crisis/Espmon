<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Espmon.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:Espmon" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" Title="Espmon" Closed="Window_Closed">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <NavigationView x:Name="navView" IsBackButtonVisible="Collapsed" PaneDisplayMode="Left" PaneClosing="navView_PaneClosing" PaneOpening="navView_PaneOpening" DisplayModeChanged="navView_DisplayModeChanged" SelectionChanged="navView_SelectionChanged" ItemInvoked="navView_ItemInvoked">
        <NavigationView.MenuItems>
            <NavigationViewItem Icon="Home" Content="Dashboard" Tag="dashboard"/>
            <NavigationViewItem Icon="SwitchApps"  Content="Screens" Tag="screens"/>
            <NavigationViewItem Icon="Link" Content="Devices" Tag="devices"/>
            <NavigationViewItem Icon="CalendarDay"  Content="Providers" Tag="providers"/>
        </NavigationView.MenuItems>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid x:Name="screensPanel" Visibility="Visible">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250" MinWidth="150"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <ListView x:Name="screenItemList" Loaded="screenItemList_Loaded"  ItemsSource="{x:Bind ViewModel.ScreenItems, Mode=OneWay}" SelectionChanged="screenItemList_SelectionChanged" SelectedIndex="{x:Bind ViewModel.SelectedScreenIndex, Mode=TwoWay}">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:ScreenListEntry">
                                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="32" Loaded="screenItem_Loaded">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" x:Name="nameDisplay" Text="{x:Bind Name}" VerticalAlignment="Center" Margin="4,0,8,0" DoubleTapped="nameDisplay_DoubleTapped"/>
                                        <TextBlock Grid.Column="0" x:Name="nameDisplayDefault" Text="{x:Bind Name}" VerticalAlignment="Center" Margin="4,0,8,0" Visibility="Collapsed"/>
                                        <TextBox Grid.Column="0" x:Name="nameEdit" Text="{x:Bind Name, Mode=TwoWay}" VerticalAlignment="Center" Margin="2,2,4,2" Visibility="Collapsed" LostFocus="NameEdit_LostFocus" KeyDown="NameEdit_KeyDown"/>
                                        <Button Grid.Column="1" Background="Transparent" BorderThickness="0" Padding="4,0" VerticalAlignment="Center" ToolTipService.ToolTip="Save" Click="SaveButton_Click">
                                            <FontIcon Glyph="&#xE74E;" FontSize="14"/>
                                        </Button>
                                        <Button Grid.Column="2" x:Name="deleteButton" Background="Transparent" BorderThickness="0" Padding="4,0" VerticalAlignment="Center" ToolTipService.ToolTip="Delete" Click="DeleteButton_Click">
                                            <FontIcon Glyph="&#xE74D;" FontSize="14"/>
                                        </Button>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                    <ScrollViewer Grid.Column="2" VerticalScrollMode="Auto" HorizontalScrollMode="Auto">
                        <local:ScreenEditor x:Name="preview" />
                    </ScrollViewer>
                </Grid>
                <Grid x:Name="devicesPanel" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250" MinWidth="150"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <ListView x:Name="devicePortsList" SelectionChanged="devicePortsList_SelectionChanged" ItemsSource="{x:Bind ViewModel.SessionEntries, Mode=OneWay}" SelectedItem="{x:Bind ViewModel.SelectedSessionEntry, Mode=TwoWay}" SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:SessionEntry">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{x:Bind Name, Mode=OneWay}" VerticalAlignment="Center">
                                            <TextBlock.Foreground>
                                                <SolidColorBrush Color="{x:Bind TextColor, Mode=OneWay}"/>
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                        <SymbolIcon Grid.Column="1" Symbol="Upload" Visibility="{x:Bind RequiresFlash, Mode=OneWay}" Foreground="Orange" Margin="8,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                    <ScrollViewer Grid.Column="2" VerticalScrollMode="Auto" HorizontalScrollMode="Auto">
                        <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <StackPanel HorizontalAlignment="Center" Visibility="{x:Bind ViewModel.SessionOpenVisibility, Mode=OneWay}" VerticalAlignment="Center" Spacing="12">
                                <TextBlock Text="Connect to device" HorizontalAlignment="Center"/>
                                <ScrollViewer x:Name="openLogScroller" VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.Log, Mode=OneWay}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <TextBlock Text="{x:Bind}" TextWrapping="NoWrap" Margin="0,2"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                                <Button x:Name="openButton" Content="Connect" HorizontalAlignment="Center" Click="openButton_Click" />
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center" Visibility="{x:Bind ViewModel.SessionRunningVisibility, Mode=OneWay}" VerticalAlignment="Center" Spacing="12">
                                <local:ScreenView x:Name="currentScreenView" Width="{x:Bind ViewModel.SelectedSession.HorizontalResolution, Mode=OneWay}" Height="{x:Bind ViewModel.SelectedSession.VerticalResolution, Mode=OneWay}"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedSession.DisplayName, Mode=OneWay}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedSessionScreenMetrics, Mode=OneWay}" HorizontalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.SelectedSession.InputType, Mode=OneWay}" HorizontalAlignment="Center"/>
                                <local:ScreenSelector x:Name="deviceScreens"  Screens="{x:Bind ViewModel.ScreenItems,Mode=OneWay}" DeviceScreens="{x:Bind ViewModel.SelectedSession.Device.Screens, Mode=TwoWay}" SelectedScreenIndex="{x:Bind ViewModel.SelectedSession.CurrentScreenIndex, FallbackValue=-1, Mode=TwoWay}" ScreenSelectionChanged="deviceScreens_ScreenSelectionChanged"/>
                                <Button Name="closeButton" Content="Disconnect" HorizontalAlignment="Center" Click="closeButton_Click"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Center" Visibility="{x:Bind ViewModel.SessionFlashVisibility, Mode=OneWay}" VerticalAlignment="Center" Spacing="12">
                                <TextBlock Text="Flash firmware to device" HorizontalAlignment="Center"/>
                                <ComboBox x:Name="flashCombo" ItemsSource="{x:Bind ViewModel.FirmwareEntries, Mode=OneWay}" PlaceholderText="Choose...">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DisplayName}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <ScrollViewer x:Name="openFlashLogScroller" VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                                    <ItemsControl ItemsSource="{x:Bind ViewModel.Log, Mode=OneWay}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="x:String">
                                                <TextBlock Text="{x:Bind}" TextWrapping="NoWrap" Margin="0,2"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                                <Button x:Name="flashButton" Content="Flash" HorizontalAlignment="Center" Click="flashButton_Click" />
                                <Button x:Name="retryConnectButton" Content="Retry Connection" HorizontalAlignment="Center" Click="retryConnectButton_Click"/>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Grid>
                <Grid x:Name="providersPanel" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250" MinWidth="150"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}">
                    </Border>
                    <ScrollViewer Grid.Column="2" VerticalScrollMode="Auto" HorizontalScrollMode="Auto"></ScrollViewer>
                </Grid>
            </Grid>
        </Grid>
    </NavigationView>
</Window>
       