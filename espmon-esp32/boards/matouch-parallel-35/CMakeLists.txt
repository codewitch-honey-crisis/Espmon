cmake_minimum_required(VERSION 3.16)

# Auto-generated by build_all.ps1 -- do not edit by hand.

# Injected by build system
add_compile_definitions(BUILD_TIMESTAMP_UTC=${BUILD_TIMESTAMP_UTC})

# Board-specific defines
add_compile_definitions(HTCW_GFX_NO_SWAP)
add_compile_definitions(MATOUCH_ESP_DISPLAY_PARALLEL_35)
add_compile_definitions(LCD_DIVISOR=20)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(espmon)
